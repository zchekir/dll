///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
//
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;
using System.Drawing;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;
using System.IO;
namespace CPA.AmazonS3
{
    public partial class DeletingExitingReport
    {
        
    	
    	String fileName = "export_";
    	
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void Deeting_ExitingReport(string ProtocolNumber, string Secret, string AuthToken, string CPADOM, string Key)
        {
           // new code
           
           var year = System.DateTime.Now.Year.ToString();
            
             string path = Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.UserProfile), "Downloads");
            // Ranorex.Core.Data.CsvDataConnector csvConnector = new
            // Ranorex.Core.Data.CsvDataConnector("myCSVConnector", path+ "\\export_20200421060500.csv",true);
           
           
             
         var listofFiles = Directory.GetFiles(path,fileName+"*"+".csv");
        	

            if (listofFiles.Length == 0)
            {
                Report.Info("No files have been found in '" + path);
            }

            foreach (string file in listofFiles)
            {
                try
                {
                    File.Delete(file);
                    Report.Info("File has been deleted: " + file.ToString());
                }
                catch (Exception ex)
                {
                    Report.Error(ex.Message);
                }
            }
        	
        	
        	
        	
        	
        	
        }

    }
}
